# data is auto-downloaded, not committed (groups.json IS committed)
data/results.csv
data/*.cache
data/live/
# FBref Big-5 stat tables (worldfootballR_data mirror) — reproducible downloads (~150MB)
data/fbref_cache/
# Transfermarkt scraped squad pages (~8MB) — reproducible; keep the small id map tracked
data/tm_cache/*.html
# penalty zone-scheme reference images (only used to decode the grid; ~10MB)
data/raw/penalty_*.png

# python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# os
.DS_Store

# dev figure outputs (canonical copies live in site/assets/worldcup/)
figures/*.png

# build artifacts
*.egg-info/

# Model-vs-Market live capture (unattended; not committed)
data/odds.sqlite
data/odds.sqlite-*
data/raw_html/
data/logs/
data/*_status.json
mlfootball/market/deploy/*.plist
data/demo.sqlite
